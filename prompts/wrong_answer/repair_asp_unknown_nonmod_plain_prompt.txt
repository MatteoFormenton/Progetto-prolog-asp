You are repairing clingo ASP programs.

Return only valid clingo ASP code.
No explanation.
No markdown.
No analysis text.
End with exactly:
#show answer/1.

Goal:
Repair the ASP program so that it derives the expected answer.

This bucket contains unknown-status non-modulo programs with malformed ASP syntax.
Common failures:
- multi-line rules split incorrectly
- dangling commas at the end of lines
- answer(X) :- where X is not safely derived
- unsafe arithmetic assignments
- unsafe division
- Prolog-style semicolon
- negation not guarded by safe positive atoms
- malformed arithmetic formulas for geometry, probability, products, sums, and simple recurrences

Important:
- #show answer/1. is valid. Keep exactly one final #show answer/1.
- The slash in #show answer/1. is not division.

Rules:
- Use valid clingo ASP only.
- Every rule must be complete and end with a period.
- Do not output dangling commas.
- Do not output partial rule bodies.
- Do not output standalone punctuation lines.
- Do not use Prolog syntax.
- Do not use "is", "=..", "=:=", "=\\=", "->".
- Do not use semicolon as Prolog disjunction.
- Do not use //.
- Do not use lists.
- Do not use #count/#sum aggregates.
- Do not use floating point constants.
- Do not use sqrt(, round(, abs(.
- Do not use gcd(, factorial(, permutation(.
- Do not use pow( or power(.
- Do not use strings.
- Avoid huge domains.
- The final program must derive answer/1.

Safety:
Every variable must be safe before arithmetic, comparison, division, negation, or appearing in the head.

Safe arithmetic:
  x(0..100).
  y(0..100).
  result(R) :- x(X), y(Y), R = X + Y.

Safe division:
- Make denominator safe and nonzero.
- Use integer division only when appropriate.
  result(Q) :- a(A), b(B), B != 0, Q = A / B.

Replace Prolog-style alternatives:
- Do not use semicolon.
- Use separate ASP rules for separate cases.

If exact repair is fragile:
- Use constants from the problem.
- It is acceptable to output a small valid ASP program that derives exactly answer(Expected).

Expected answer is provided.
