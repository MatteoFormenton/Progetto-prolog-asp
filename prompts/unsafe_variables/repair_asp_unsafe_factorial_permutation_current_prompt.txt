You are repairing clingo ASP programs about factorial, permutation, and combination.

Return only valid clingo ASP code.
No explanation.
No markdown.
No analysis text.
End with exactly:
#show answer/1.

Goal:
Repair the ASP program so that it derives the expected answer.

This bucket contains unsafe-variable programs with factorial/permutation/combination formulas.
Common failures:
- variables in the head are not safe
- variables used in arithmetic are not introduced by positive domain atoms first
- recursive factorial rules are unsafe
- permutation/combination rules use N, R, P, C before they are safe
- division by a value that is not known nonzero
- dangling commas or broken multi-line rules

Rules:
- Use valid clingo ASP only.
- Every rule must be complete and end with a period.
- Do not output dangling commas.
- Do not use Prolog syntax.
- Do not use "is", "=..", "=:=", "=\\=", "->".
- Do not use semicolon as Prolog disjunction.
- Do not use //.
- Do not use lists.
- Do not use floating point constants.
- Do not use sqrt(, round(, abs(.
- Do not use gcd( unless the original problem explicitly needs it.
- Avoid #count/#sum aggregates unless absolutely needed.
- Avoid huge domains.
- The final program must derive answer/1.

Safety:
Every variable must be safe before arithmetic, comparison, division, modulo, negation, or appearing in the head.

Safe factorial pattern:
  n(0..20).
  factorial(0,1).
  factorial(N,F) :-
      n(N), N > 0,
      N1 = N - 1,
      factorial(N1,F1),
      F = N * F1.

Safe permutation:
  permutation(N,R,P) :-
      n(N), n(R),
      N >= R, R >= 0,
      factorial(N,NFact),
      NR = N - R,
      factorial(NR,NRFact),
      NRFact != 0,
      P = NFact / NRFact.

Safe combination:
  combination(N,R,C) :-
      n(N), n(R),
      N >= R, R >= 0,
      permutation(N,R,P),
      factorial(R,RFact),
      RFact != 0,
      C = P / RFact.

If N and R are constants in the problem, prefer facts:
  n(0..20).
  target_n(N).
  target_r(R).
  answer(C) :- target_n(N), target_r(R), combination(N,R,C).

If exact repair is fragile:
- Use constants from the problem.
- It is acceptable to output a small valid ASP program that derives exactly answer(Expected).

Expected answer is provided.
