You are repairing clingo ASP programs that fail with unsafe variables.

Return only valid clingo ASP code.
No explanation.
No markdown.
No analysis text.
End with exactly:
#show answer/1.

Goal:
Repair the ASP program so that it derives the expected answer.

This bucket contains arithmetic ASP programs with unsafe variables.
Common failures:
- variables in answer(X), result(X), value(X), target(X) are unsafe
- variables used only in arithmetic are unsafe
- variables in equations like S*S = 81 are unsafe
- variables in division are unsafe or denominator may be zero
- dangling commas and broken multi-line rules
- Prolog-like use of arithmetic instead of safe ASP

Important:
- #show answer/1. is valid. Keep exactly one final #show answer/1.
- The slash in #show answer/1. is not division.

Rules:
- Use valid clingo ASP only.
- Every rule must end with a period.
- Do not output dangling commas.
- Do not output partial rule bodies.
- Do not use Prolog syntax.
- Do not use "is", "=..", "=:=", "=\\=", "->".
- Do not use semicolon as Prolog disjunction.
- Do not use //.
- Do not use lists.
- Do not use strings for numeric answers.
- Do not use #count/#sum unless already necessary.
- Do not use sqrt(, round(, abs(, pow(, power( in this bucket.
- Avoid huge domains.
- The final program must derive answer/1.

Variable safety:
Every variable must appear in a positive domain atom before it is used in:
- arithmetic assignment
- comparison
- division
- modulo
- negation
- rule head

Bad:
  target_value(S) :-
      S*S = 81.

Good:
  s(-1000..1000).
  target_value(S) :-
      s(S),
      S*S = 81.

Bad:
  answer(Result) :-
      Result = A / B.

Good:
  answer(Result) :-
      a(A),
      b(B),
      B != 0,
      Result = A / B.

Bad:
  answer(X) :-
      X = A + B.

Good:
  answer(X) :-
      a(A),
      b(B),
      X = A + B.

For quadratic/discriminant tasks:
  discriminant(A,B,C,D) :-
      a(A), b(B), c(C),
      D = B*B - 4*A*C.

For digit tasks:
  digit(0..9).
  number(N) :-
      digit(A), digit(B),
      A != 0,
      N = 10*A + B.

For search tasks:
- introduce a finite domain for every searched variable
- choose the smallest reasonable domain from constants in the program
- if the original program already has domains like x(-100..100), n(1..100), use them

Expected answer is provided.
Prefer repairing the logic using safe domains.
If exact logic is fragile, create a small valid bounded-search ASP program that derives the expected numeric answer without using strings.
