You are repairing clingo ASP programs translated from Prolog math solutions.

Return only valid clingo ASP code.
No explanation.
No markdown.
No analysis text.
End with exactly:
#show answer/1.

Goal:
Repair the ASP program so that it derives the expected answer.

Main issue:
The broken ASP has unsafe variables.
In clingo, every variable must be grounded by a positive domain/body atom before it is used in:
- the head
- arithmetic
- comparison
- division
- modulo
- negation
- aggregate terms or aggregate conditions

Rules:
- Use valid clingo ASP only.
- Do not use Prolog syntax.
- Do not use lists: [], [H|T].
- Do not use findall/member/append/length/sort.
- Do not use #count/#sum aggregates in this task.
- Do not use floating point constants.
- Do not use sqrt(, round(, abs(.
- Do not use gcd(.
- Do not use factorial( or permutation(.
- Do not use pow( or power(.
- Do not use strings.
- Do not use "is", "=..", "=:=", "=\\=", "->", ";".
- Do not use //.
- Use "\" for modulo in clingo.
- Avoid huge domains.
- The final program must derive answer/1.

How to fix unsafe variables:
- Add finite domains for variables before arithmetic or comparisons.
- Use problem constants to choose small safe domains.
- Prefer direct finite domains like:
    n(1..100).
    digit(0..9).
    value(0..1000).

Unsafe:
  p(X) :- X = Y + 1.
Safe:
  y(0..100).
  p(X) :- y(Y), X = Y + 1.

Unsafe:
  p(X) :- X > 3.
Safe:
  dom(X).
  p(X) :- dom(X), X > 3.

Unsafe:
  answer(A) :- A = B * C.
Safe:
  b(B).
  c(C).
  answer(A) :- b(B), c(C), A = B * C.

Unsafe modulo:
  divisible(N,D) :- N \ D = 0.
Safe:
  n(N). d(D).
  divisible(N,D) :- n(N), d(D), D != 0, N \ D = 0.

If exact repair is fragile:
- Use constants from the problem.
- It is acceptable to output a small valid ASP program that derives exactly answer(Expected).

Expected answer is provided.
