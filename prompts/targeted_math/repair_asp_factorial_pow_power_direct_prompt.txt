You are converting Prolog math solutions to clingo ASP.

Return only valid clingo ASP code.
No explanation.
No markdown.
No analysis text.
End with exactly:
#show answer/1.

Goal:
Produce an ASP program that derives the expected answer.

Rules:
- Do not output only "#show answer/1.".
- Do not use Prolog syntax.
- Do not use lists: [], [H|T].
- Do not use findall/member/append/length/sort.
- Do not use "is", "=..", "=:=", "=\\=", "->", ";".
- Do not use //.
- Use "\" for modulo in clingo.
- Do not use unsafe variables.
- Avoid recursion over large exponents.
- Avoid huge domains.
- Prefer direct finite arithmetic from constants in the problem.
- It is acceptable to simplify the math using constants from the Prolog program.
- The final output must derive answer/1.

For factorial:
- If N is small, either use direct facts or a small finite domain 0..N.
- Safe pattern:
  n(0..N).
  fact(0,1).
  fact(I,F) :- n(I), I > 0, I1 = I - 1, fact(I1,F1), F = I * F1.
- Do not recurse without a finite domain.

For powers:
- If exponent is small, compute directly.
- If exponent is large and modulo is involved, use modular cycles.
- Do not recursively decrement a huge exponent.
- Do not generate large ranges.

For formulas:
- Use integer arithmetic over constants.
- Use "/" for integer division.
- Keep intermediate variables safe.

Problem:
{input}

Original Prolog:
{prolog_program}

Expected answer:
{answer}

Previous broken ASP:
{asp_program}
