You are repairing clingo ASP programs translated from Prolog math solutions.

Return only valid clingo ASP code.
No explanation.
No markdown.
No analysis text.
End with exactly:
#show answer/1.

Goal:
Repair the ASP program so that it derives the expected answer.

Main issue:
The broken ASP uses factorial/2, permutation/2, or combinatorics rules with unsafe variables, bad recursion, or Prolog-style syntax.

Rules:
- Use valid clingo ASP only.
- Do not use Prolog syntax.
- Do not use lists: [], [H|T].
- Do not use findall/member/append/length/sort.
- Do not use #count/#sum aggregates in this task.
- Do not use pow( or power(.
- Do not use gcd(.
- Do not use floating point constants.
- Do not use sqrt(, round(, abs(.
- Do not use strings.
- Do not use "is", "=..", "=:=", "=\\=", "->", ";".
- Do not use //.
- Use "\" for modulo in clingo.
- Every variable must be safe before arithmetic, comparison, division, modulo, negation, or appearing in the head.
- Avoid huge domains.
- The final program must derive answer/1.

Safe factorial pattern:
  n(0..MAX).
  factorial(0,1).
  factorial(N,F) :- n(N), N > 0, N1 = N - 1, factorial(N1,F1), F = N * F1.

Safe permutation pattern:
  n(0..MAX).
  factorial(0,1).
  factorial(N,F) :- n(N), N > 0, N1 = N - 1, factorial(N1,F1), F = N * F1.
  permutation(N,R,P) :- n(N), n(R), N >= R, NR = N - R, factorial(N,FN), factorial(NR,FNR), P = FN / FNR.

Safe combination pattern:
  combination(N,R,C) :- n(N), n(R), N >= R, NR = N - R,
      factorial(N,FN), factorial(R,FR), factorial(NR,FNR), C = FN / (FR * FNR).

For small constants:
- Prefer deriving the needed factorial/permutation/combinatorics value directly.
- If recursion becomes fragile or creates huge search, simplify using the expected answer.

Expected answer is provided.
It is acceptable to output a small valid ASP program that derives exactly answer(Expected).
