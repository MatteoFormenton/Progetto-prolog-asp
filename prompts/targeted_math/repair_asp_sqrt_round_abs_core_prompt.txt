You are repairing clingo ASP programs translated from Prolog math solutions.

Return only valid clingo ASP code.
No explanation.
No markdown.
No analysis text.
End with exactly:
#show answer/1.

Goal:
Repair the ASP program so that it derives the expected answer.

Important:
- clingo does not support arbitrary sqrt(...), round(...), or abs(...) as normal math functions.
- Do not use sqrt(.
- Do not use round(.
- Do not use abs(.
- Replace them with integer arithmetic, finite cases, squared comparisons, or direct constants.
- If exact symbolic repair is fragile, it is acceptable to simplify using constants from the problem and derive answer(Expected).

Rules:
- Do not output only "#show answer/1.".
- Do not use Prolog syntax.
- Do not use lists: [], [H|T].
- Do not use findall/member/append/length/sort.
- Do not use strings or string concatenation.
- Do not use floating-point constants such as 1.0, 0.5, 3.14.
- Do not use "is", "=..", "=:=", "=\\=", "->", ";".
- Do not use //.
- Use "\" for modulo in clingo.
- Every variable must be safe before arithmetic, comparison, modulo, division, negation, or appearing in the head.
- Avoid huge domains.
- Prefer small finite integer domains from constants in the problem.
- The final program must derive answer/1.

For absolute value:
Use case rules:
  abs_value(X,X) :- dom(X), X >= 0.
  abs_value(X,Y) :- dom(X), X < 0, Y = -X.

For square roots:
- If checking sqrt(N) = R, use R*R = N.
- If sqrt is only needed for a known constant, replace it with the known integer/rational value when exact.
- If the expression is approximate or irrational, simplify directly to the expected answer.

For rounding:
- Avoid round(...).
- If the rounded result is known from the problem constants, derive answer(Expected).
- For simple integer division, use quotient bounds instead of float rounding.

For geometry/distance:
- Prefer squared distance instead of square root:
  distance2 = DX*DX + DY*DY
- Compare squared distances when possible.

For final answer:
- The expected answer is provided.
- It is acceptable to output a small ASP program that derives exactly answer(Expected), as long as it is valid clingo ASP.
